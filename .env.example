# Copy to .env and fill in. .env is gitignored.

# Kaiterra API key (https://dashboard.kaiterra.com -> API keys)
KAITERRA_API_KEY=

# Met Office Weather DataHub key (Site Specific / "Global Spot" subscription).
# Optional — the app runs on Open-Meteo alone until this is set.
METOFFICE_API_KEY=
